# Lab 01 — Getting Started with Linux

This folder contains screenshots of the Linux commands practiced during **Lab 01: Getting Started with Linux**.

## Screenshots

The screenshots are organized by command/topic:

1. **ls commands** — `ls`, `ls -a`, `ls -l`, `ls -lh`, `ls -ah`
2. **date command**
3. **Input/Output & Output Redirection**
4. **cal command**
5. **cat command**
6. **cd commands** — `cd`, `cd ..`, `cd ~`, `cd -`, `cd <directory>`
7. **clear command**
8. **echo command**
9. **expr command**
10. **figlet command**
11. **head command**
12. **pwd command**
13. **tail command**

All screenshots are stored inside the [`Screenshots`](./Screenshots) folder.
